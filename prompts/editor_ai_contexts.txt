editor_version2.html:

Focus on the folder @editor_version2.
This is a json editor that edits a json file (an object in the code) that stores information about a subway network. The network consists of lines, which consist of branches, which consist of stations. Each station might have checkpoints. All information can be edited. In addition, the positions (latitude, longtitudes) of stations can be edited by clicking on a map. There are also many different keys one can press to edit/delete stations and checkpoints.

Rules:
Only do the things specified in the prompt. Do not change parts of the code that I did not tell you to change.

Task:

In @editor_version2/editor_version2.html, the object "this" currently stores all of the markers in this.markers and all of the polylines in this.polylines. The updateMap function updates the map by deleting all of the markers and polylines and then re-drawing them again.

To improve performance, change it such that the markers and polylines are stored by line and branch, meaning that each branch of each line has its markers and polylines stored separately. Next, change updateMap such that it only re-draws the markers and polylines of the branch that changed. For example, in insertCheckpointAt, only update the markers and polylines of the branch that the checkpoint being inserted was in. Make the function require the line ID and branch ID as parameters. 

Next, change the updateMap function in the places where it gets executed (i.e. add the line ID and branch ID parameters). In places where all of the branches are updated (e.g. when the key 'u' is pressed, it updates the entire map), run updateMap for all of the branches. 

Make sure that the code can run in the end.
