editor_version1.html:

Focus on the folder @editor_version1.
This is a json editor that edits a json file (an object in the code) that stores information about a subway network. The network consists of lines, which consist of branches, which consist of stations. All information can be edited. In addition, the positions (latitude, longtitudes) of stations can be edited by clicking on a map.

Rules:
Only do the things specified in the prompt. Do not change parts of the code that I did not tell you to change.

Task:
In @editor_version1/editor_version1.html, add a "settings" screen that the user can toggle to show/hide (add a switch at the top to do this). When the "settings" screen gets shown, the data about the lines and branches gets hidden, and the content in the "settings" screen replaces it (However, don't cover the map. Only hide the left side showing the lines and branches.). In the "settings" screen, list out all of the lines in this.lines, and add a toggling switch between "show/hide" for each line. If a line is in "hide" mode, it doesn't get shown on the map on the right; If a line is in "show" mode, it gets shown. Also, in the settings, next to each line, add a "z-index" that the user can input. This z-index determines whether the lines get shown on the front/back on the map, with higher z-index lines being shown on the front/top. Make sure that this z-index variable is stored in this.lines, as I will need to export the z-index values as part of the data.

Make sure that the code can run in the end.
